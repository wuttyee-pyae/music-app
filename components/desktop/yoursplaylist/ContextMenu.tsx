import React from 'react'

interface ContextMenuProp {
    x: number
    y: number
}

const ContextMenu = ({x, y}) => {
  return (
    <div>ContextMenu</div>
  )
}

export default ContextMenu